# Leave unset to use the Vite dev-server proxy (recommended for local dev).
# Set this when the API is served from a different origin, e.g. in production.
# VITE_API_BASE_URL=http://localhost:5000/api

# Target the dev-server proxy forwards /api to.
VITE_API_PROXY_TARGET=http://localhost:5000
